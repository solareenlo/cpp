#include <iostream>
#include <map>
#include <set>
#include <vector>
#define REP(i, n) for (int i = 0; i < (n); i++)
using namespace std;


void solve(long long S, long long K){

}

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);
    long long S;
    scanf("%lld",&S);
    long long K;
    scanf("%lld",&K);
    solve(S, K);
    return 0;
}
