#include <iostream>
#include <map>
#include <set>
#include <vector>
#define REP(i, n) for (int i = 0; i < (n); i++)
using namespace std;


void solve(std::string ABCD){

}

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);
    std::string ABCD;
    std::cin >> ABCD;
    solve(ABCD);
    return 0;
}
