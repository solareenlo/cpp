#include <iostream>
#include <map>
#include <set>
#include <vector>
#define REP(i, n) for (int i = 0; i < (n); i++)
using namespace std;


void solve(long long S){

}

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);
    long long S;
    scanf("%lld",&S);
    solve(S);
    return 0;
}
